
  # TCDA

  This is a code bundle for TCDA. The original project is available at https://www.figma.com/design/HyNBNHaR26RiixBaa7lTTS/TCDA.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  